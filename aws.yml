---
test-instance:
  region: us-east-1
  availability_zone: us-east-1a
  ami_id: ami-9887c6e7
  instance_type: t2.xlarge
  tags:
    Name: terraform-consul
hdp:
  cluster-minimal: /resources/hdp-configs/hdp-cluster-minimal.yml
  cluster-bigsql: /resources/hdp-configs/hdp-cluster-bigsql.yml
  cluster-hadoop3: /resources/hdp-configs/hdp-cluster-hadoop3.yml
  single-minimal: /resources/hdp-configs/hdp-single-minimal.yml
  single-hadoop3: /resources/hdp-configs/hdp-single-hadoop3.yml
  no_instances: 2
  cluster_name: hdp-bigsql
  master-clients: ['ZOOKEEPER_CLIENT', 'HDFS_CLIENT', 'YARN_CLIENT', 'MAPREDUCE2_CLIENT', 'HIVE_CLIENT']
  master-services: "- ZOOKEEPER_SERVER
      - NAMENODE
      - SECONDARY_NAMENODE
      - RESOURCEMANAGER
      - APP_TIMELINE_SERVER
      - HISTORYSERVER
      - HIVE_SERVER
      - HIVE_METASTORE
      - WEBHCAT_SERVER
      - AMBARI_SERVER
      - METRICS_COLLECTOR
      - METRICS_MONITOR
      - HST_SERVER"
